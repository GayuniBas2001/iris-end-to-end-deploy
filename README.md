# mnist-end-to-end-deploy
An end-to-end demo of deploying a simple MNIST digit classifier. Includes PyTorch training, FastAPI inference endpoint, Docker containerization, and testing via Postman. Shows full pipeline: train → save model → load for inference → serve predictions as an API.
